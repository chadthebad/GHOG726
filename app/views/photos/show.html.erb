<p id="notice"><%= notice %></p>

<h3>
  <%= @photo.title %> from: 
  <%= link_to @photo.album.title, @photo.album %>
</h3>

<img src="/uploads/<%= @photo.id %>.jpg">

<p>
  Uploaded by:
  <%= link_to @photo.album.user.name, @photo.album.user %>
</p>


<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', photos_path %>
